
# Borradores de docs/proposals que no van al repo. Con ignorarlos basta: se han
# colado dos veces por un `git add docs` bienintencionado, y una regla vale más
# que acordarse.
docs/proposals/*.zip
docs/proposals/rec.mjs

# Handoff de diseño descomprimido: se mira, no se versiona.
.diseno-voz/

# Binarios del probe del webview: se compilan con una línea, no se versionan.
spikes/webview-probe/probe
spikes/webview-probe/bench
